import React from "react";
import PublicNavBar from "../components/PublicNavBar";

const SearchList = () => {
  return (
    <div>
      <PublicNavBar />
    </div>
  );
};

export default SearchList;
